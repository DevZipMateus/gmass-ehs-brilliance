// Minimal fallback types for Supabase client.
// The generated types file is missing in this project, so we provide a lightweight substitute
// to satisfy TypeScript module resolution.

export type Database = any;
